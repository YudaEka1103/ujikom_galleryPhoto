<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photo</title>
    <!-- Include Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white">
    <div class="flex flex-col">
        <%- include('./partials/navbar.ejs') %>
    <div class="container mx-auto my-10 px-10">
        <h2 class="text-2xl font-bold mb-4">Photos</h2>
        <% if (fotos.length === 0) { %>
            <div class="flex flex-col items-center justify-center py-32">
                <div class="text-center mb-8">
                    <p class="text-2xl font-semibold text-gray-800">Belum ada foto, ingin menambahkan?</p>
                </div>
                <div> 
                    <a href="/profile/photo/upload" class=" px-6 py-3 bg-green-500 text-white font-semibold rounded-md shadow-md hover:bg-green-600 transition duration-300 flex flex-cols"><?xml version="1.0" encoding="UTF-8"?><svg width="24px" height="24px" stroke-width="1.5" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" color="#000000"><path d="M6 12H12M18 12H12M12 12V6M12 12V18" stroke="#000000" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path></svg>Tambahkan Foto</a>
                </div>
            </div>            
            
        <% } else { %>
            <div class="flex items-center justify-between my-5">
                <p class="text-2xl font-semibold text-gray-800">Photo</p>
                <a href="/profile/photo/upload" class="bg-green-500 hover:bg-green-600 transition duration-300 text-white font-semibold rounded-md shadow-md py-3 px-6 flex flex-cols">
                    <?xml version="1.0" encoding="UTF-8"?><svg width="24px" height="24px" stroke-width="1.5" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" color="#000000"><path d="M6 12H12M18 12H12M12 12V6M12 12V18" stroke="#000000" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path></svg>Tambahkan Foto</a>
            </div>
            
            <!-- Loop untuk menampilkan setiap foto -->
            
            <section class="container mx-auto px-10 my-10">
                <div class="mx-auto">
                    <div class="sm:columns-2 md:columns-3 lg:columns-4 gap-3">
                        <% fotos.forEach(foto => { %>
                            <div class="relative my-3">
                                <a href="/post/<%= foto._id %>" class="block relative group">
                                    <img src="/uploads/<%= foto.lokasifile %>" alt="<%= foto.judulfoto %>" class="object-cover w-full h-full transition duration-300 ease-in-out transform group-hover:opacity-80 hover:scale-105" />
                                    <div class="absolute inset-0 p-4 bg-black bg-opacity-70 transition duration-300 ease-in-out opacity-0 group-hover:opacity-100 text-white flex flex-col justify-end">
                                        <p class="mb-2"><%= foto.userid.username %></p>
                                        <p><%= foto.judulfoto %></p>
                                    </div>
                                </a>
                            </div>
                        <% }); %>
                    </div>
                </div>
            </section>
            
        <% } %>
    </div>
    </div>
    
</body>
</html>


